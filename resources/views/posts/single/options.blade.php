<div style="width:95%;padding-right:20px;">
	{{--
	<h2>Opciones</h2>

	@if ($post->kpost)
	  @if ($post->featured)
	    <div class="image-w-text">
	      {{ $opc_featured }}
	    </div>
	  @endif
	@endif

	@if ($post->cstr_privacy)
	  <div class="image-w-text">
	    {{ $opc_privacy }}
	  </div>
	@endif

	@if ($post->cstr_restricted)
	  <div class="image-w-text">
	    {{ $opc_restricted }}
	  </div>
	@endif

	@if ($post->cstr_allow_comments)
	  <div class="image-w-text">
	    Cualquier usuario puede realizar comentarios.
	  </div>
	@endif  

	@if ($post->isCatalog())
	  @if ($post->cstr_colaborative)
	    <div class="image-w-text">
	      Cualquier usuario agregar posts al catálogo.
	    </div>
	  @endif
	@endif

	@if ($post->isPage())
	  @if ($post->cstr_colaborative)
	    <div class="image-w-text">
	      Cualquier usuario agregar catálogos a la página.
	    </div>
	  @endif
	  
	  @if ($cstr_allow_subscribers)
	    <div class="image-w-text">
	      Permitir subscriptores.
	    </div> 
	  @endif

	  @if ($cstr_show_subscribers)
	    <div class="image-w-text">
	      Mostrar subscriptores. 
	    </div>
	  @endif

	  @if ($cstr_main_page)
	    <div class="image-w-text">
	      Página principal.
	    </div> 
	  @endif
	@endif
	--}}
</div>
